{
  "name": "address-verification-perf-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright performance test suite for the AddressVerification C# API",
  "scripts": {
    "test": "playwright test",
    "test:latency": "playwright test tests/02-verify-latency.spec.ts",
    "test:throughput": "playwright test tests/03-verify-throughput.spec.ts",
    "test:soak": "playwright test tests/06-mixed-soak.spec.ts",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
